[package]
name = "vesto-python"
version = "0.1.0"
edition = "2024"

[lib]
name = "vesto"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = {version = "0.29.2", features = ["extension-module"]}
ndarray = "0.17.2"

vesto-core = {path = "../vesto-core"}
pyo3-stub-gen = "0.23.0"
